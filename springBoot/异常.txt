11:16:45.511 [main] ERROR org.springframework.boot.SpringApplication - Application run failed
java.lang.IllegalArgumentException: Invalid profile []: must contain text：
	配置文件加载异常,profile加载失败。
	spring:
	  profiles:
		include: business,
	去掉逗号即可
The content of element type "mapper" must match "(cache-ref|cache|resultMap*|parameterMap*|sql*|
 insert*|update*|delete*|select*)+". 
	这个错误的意思是 你写的语句或者标签和mybatis的标准化配置不匹配。
	如果 检查之后还是没找到错误的话，重启一下ecplise试试。
idea中无法debug springBoot的启动类：
	第一次导入的项目要先进行maven的编译安装，否则无法进行debug。
